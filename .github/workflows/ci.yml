name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_TOOLCHAIN: "1.91.1"

jobs:
  quality:
    name: Rust quality and tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Compute dependency cache key
        id: dependency-cache
        run: echo "key=$(.github/scripts/dependency-cache-key.sh)" >>"$GITHUB_OUTPUT"

      - name: Restore pinned Rust toolchain cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: ~/.rustup/toolchains/${{ env.RUST_TOOLCHAIN }}-*
          key: ${{ runner.os }}-${{ runner.arch }}-rust-toolchain-${{ env.RUST_TOOLCHAIN }}-quality-components

      - name: Install pinned Rust toolchain
        run: |
          rustup toolchain install "$RUST_TOOLCHAIN" --profile minimal
          rustup component add rustfmt clippy --toolchain "$RUST_TOOLCHAIN"

      - name: Restore shared Cargo dependency cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-rust-deps-${{ env.RUST_TOOLCHAIN }}-${{ steps.dependency-cache.outputs.key }}
          restore-keys: |
            ${{ runner.os }}-rust-deps-${{ env.RUST_TOOLCHAIN }}-

      - name: Restore ripgrep tool cache
        id: ripgrep-cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: ~/.local/bin/rg
          key: ${{ runner.os }}-${{ runner.arch }}-ripgrep-14.1.0

      - name: Install command-line test dependencies
        run: |
          if [ ! -x "$HOME/.local/bin/rg" ]; then
            sudo apt-get update -qq
            sudo apt-get install -y --no-install-recommends ripgrep
            mkdir -p "$HOME/.local/bin"
            install -m 0755 /usr/bin/rg "$HOME/.local/bin/rg"
          fi
          echo "$HOME/.local/bin" >>"$GITHUB_PATH"

      - name: Check formatting
        run: cargo +"$RUST_TOOLCHAIN" fmt --all -- --check

      - name: Run Clippy
        run: cargo +"$RUST_TOOLCHAIN" clippy --all-targets --all-features --locked -- -D warnings

      - name: Run tests
        env:
          CODEX_SSH_BRIDGE_PERSISTENT_HELPER_INTEGRATION: "1"
        run: cargo +"$RUST_TOOLCHAIN" test --all-targets --all-features --locked

      - name: Validate source package metadata
        shell: bash
        run: |
          set -euo pipefail
          test ! -e bin/codex-ssh-bridge
          test ! -e .mcp.json
          test -f .mcp.json.example
          cargo_version="$(cargo +"$RUST_TOOLCHAIN" metadata --no-deps --format-version 1 | jq -r '.packages[0].version')"
          plugin_version="$(jq -r '.version' .codex-plugin/plugin.json)"
          test "$cargo_version" = "$plugin_version"
          jq -e '.mcpServers == null' .codex-plugin/plugin.json >/dev/null
          jq -e '.mcpServers["ssh-bridge"].args == ["mcp"]' .mcp.json.example >/dev/null

  diagnostics:
    name: Release profile and RSS diagnostics
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Compute dependency cache key
        id: dependency-cache
        run: echo "key=$(.github/scripts/dependency-cache-key.sh)" >>"$GITHUB_OUTPUT"

      - name: Restore pinned Rust toolchain cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: ~/.rustup/toolchains/${{ env.RUST_TOOLCHAIN }}-*
          key: ${{ runner.os }}-${{ runner.arch }}-rust-toolchain-${{ env.RUST_TOOLCHAIN }}-minimal

      - name: Install pinned Rust toolchain
        run: rustup toolchain install "$RUST_TOOLCHAIN" --profile minimal

      - name: Restore shared Cargo dependency cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-rust-deps-${{ env.RUST_TOOLCHAIN }}-${{ steps.dependency-cache.outputs.key }}
          restore-keys: |
            ${{ runner.os }}-rust-deps-${{ env.RUST_TOOLCHAIN }}-

      - name: Restore diagnostics target cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: target
          key: ${{ runner.os }}-${{ runner.arch }}-rust-target-diagnostics-${{ env.RUST_TOOLCHAIN }}-${{ steps.dependency-cache.outputs.key }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-rust-target-diagnostics-${{ env.RUST_TOOLCHAIN }}-

      - name: Restore ripgrep tool cache
        id: ripgrep-cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: ~/.local/bin/rg
          key: ${{ runner.os }}-${{ runner.arch }}-ripgrep-14.1.0

      - name: Install command-line test dependencies
        run: |
          if [ ! -x "$HOME/.local/bin/rg" ]; then
            sudo apt-get update -qq
            sudo apt-get install -y --no-install-recommends ripgrep
            mkdir -p "$HOME/.local/bin"
            install -m 0755 /usr/bin/rg "$HOME/.local/bin/rg"
          fi
          echo "$HOME/.local/bin" >>"$GITHUB_PATH"

      - name: Install real SSH test dependency
        run: |
          if [ ! -x /usr/sbin/sshd ]; then
            sudo apt-get update -qq
            sudo apt-get install -y --no-install-recommends openssh-server
          fi
          sudo install -d -m 0755 /run/sshd

      - name: Prepare release diagnostic binaries
        run: |
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked --no-run \
            --lib \
            --test mcp_tools \
            --test performance_acceptance \
            --test mcp_protocol \
            --test real_ssh \
            --test remote_ops

      - name: Collect cold/warm and edit-cache profiles
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p diagnostics
          CODEX_SSH_BRIDGE_PROFILE=1 \
            cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked \
            --test performance_acceptance task11_release_cold_and_warm_ssh_profile \
            -- --nocapture \
            >diagnostics/profile.stdout \
            2>diagnostics/profile.jsonl
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked \
            --test performance_acceptance task13_release_edit_cache_latency_profile \
            -- --nocapture 2>&1 | tee diagnostics/profile-edit-cache.log

      - name: Collect release RSS diagnostics
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p diagnostics
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked \
            --test performance_acceptance task13_release_edit_cache_rss_fresh_child \
            -- --nocapture 2>&1 | tee diagnostics/rss-edit-cache.log
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked --test mcp_tools task8_output_rss_ \
            -- --nocapture 2>&1 | tee diagnostics/rss-mcp.log
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked --test performance_acceptance \
            task11_release_bounded_session_output_rss_fresh_child -- --nocapture \
            2>&1 | tee diagnostics/rss-session.log
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked --test performance_acceptance \
            task11_release_zero_output_concurrent_rss_fresh_child -- --nocapture \
            2>&1 | tee diagnostics/rss-zero-output.log
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked --test mcp_protocol task7_wide_json_rss_ \
            -- --nocapture 2>&1 | tee diagnostics/rss-json.log
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked --lib \
            remote::run::tests::task78_base64_admission_release_rss -- --nocapture \
            2>&1 | tee diagnostics/rss-base64.log
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked --test remote_ops \
            five_hosts_successfully_stream_forty_mib_below_rss_bound -- --nocapture \
            2>&1 | tee diagnostics/rss-fixed-operations.log

      - name: Run required real SSH edit-cache integration
        env:
          CODEX_SSH_BRIDGE_REQUIRE_REAL_SSH: "1"
        run: |
          cargo +"$RUST_TOOLCHAIN" test --release --features profile --locked \
            --test real_ssh \
            real_localhost_sshd_covers_transport_shell_files_mutation_and_cancellation \
            -- --nocapture

      - name: Upload diagnostic evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-diagnostics
          path: diagnostics/
          if-no-files-found: warn
          retention-days: 14
